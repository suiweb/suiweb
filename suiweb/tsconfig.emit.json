{
    // This tsconfig can be used to generate the typings for the src folder
    "extends": "./tsconfig.json",
    "include": ["src/**/*.ts"],
    "exclude": ["src/index.ts"],
    "compilerOptions": {
        "noEmit": false,
        "declaration": true,
        "outDir": "dist/js"
    }
}
